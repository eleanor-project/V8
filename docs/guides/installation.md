# Installation

Use the standard install guide in `docs/INSTALL.md`. This page summarizes the quick
steps and points to the full guide.

```bash
git clone https://github.com/eleanor-project/v8.git
cd v8
pip install -r requirements.txt
```

For full instructions and optional stacks, see `docs/INSTALL.md`.
