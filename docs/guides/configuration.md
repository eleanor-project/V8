# Configuration

Configuration is described in `docs/configuration.md` and related guides.

Key config files:

- `config/defaults.yaml`
- `config/gpu.yaml`
- `config/security.yaml`

Environment variable overrides live under the `ELEANOR_` namespace. See
`docs/configuration.md` and `docs/CONFIGURATION_SECURITY_GUIDE.md`.
