# Changelog

Release notes are tracked in `RELEASE_NOTES.md` at the repo root.

## Versioning

- The package version is defined in `pyproject.toml`.
- Update both `pyproject.toml` and `RELEASE_NOTES.md` for each release.
