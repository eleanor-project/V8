# GPU Acceleration

This example demonstrates GPU capability checks and integration.

## Quick check

```bash
python examples/gpu_example.py
```

## Integration example

```bash
python examples/gpu_integration_example.py
```

## Notes

- GPU configuration lives in `config/gpu.yaml` and `engine/config/settings.py`.
- For setup guidance, see `docs/GPU_QUICK_START.md` and
  `docs/GPU_ACCELERATION.md`.
