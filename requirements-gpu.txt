# ELEANOR V8 - GPU Acceleration Dependencies

# Core GPU support
torch>=2.1.0
torchvision>=0.16.0

# GPU utilities
nvidia-ml-py3>=7.352.0  # GPU monitoring (NVIDIA only)
gputil>=1.4.0  # Cross-platform GPU utilities
